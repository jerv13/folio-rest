module.exports = function(Content) {

    //var uuid = require('uuid');

    console.log(Content);
    /*
    Content.beforeCreate(
        'create',
        function(context, user, next) {
        var req = context.req;
        req.body.id = uuid.v4();
        req.body.date = Date.now();
        next();
    });
    */
};
